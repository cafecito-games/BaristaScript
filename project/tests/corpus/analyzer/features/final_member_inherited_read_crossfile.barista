# Reading an inherited final declared in a cross-file base is allowed.
extends "res://tests/corpus/analyzer/features/final_member_inherited_readable_base.notest.barista"

func plus_one() -> int:
	return id + 1

func test() -> void:
	print(plus_one())
